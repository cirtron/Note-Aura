# Progress ledger — Facebook capture + Stop button

Plan: docs/superpowers/plans/2026-06-17-facebook-capture-and-stop-processing.md
Execution: subagent-driven, no-git (checkpoint = `go build ./...` + task tests green).

- [x] Task 1: db stopped column + StopNote/ClearStopped/DeleteJobsForNote — review clean
- [x] Task 2: worker cancel registry — review clean
- [x] Task 3: stop endpoint + retry-clears-stopped + Stop/Stopped UI + i18n — review clean
- [x] Task 4: IsFacebook + validVTT helpers — review clean
- [x] Task 5: FetchFacebook (yt-dlp + headless + OG fallback) — review clean
- [x] Task 6: admin facebook.cookies setting — review clean (textarea confirmed inside /admin form)
- [x] Task 7: worker materialize FB branch + facebook badge — review clean (full suite green)
- [x] Task 8: smoke test + docs + CHANGELOG — review clean (CHANGELOG deduped, docs synced incl. zh-Hant, Stop smoke passed, real DB untouched)

## Log
- Task 1: complete, review clean. Minor (non-issue, no fix): notes DDL places `stopped` before migrated `ai_ms` physically; harmless because all SELECTs use named `noteCols` order.
- Task 2: complete, review clean. Concurrency verified by inspection (`-race` unavailable, CGO off on Windows host).
- Task 3: complete, review clean. Minors (no fix, within scope): retryNote calls ClearStopped unconditionally (no-op on unstopped); stopNote doesn't pre-check 'processing' state (spec doesn't require it).
- Task 4: complete, review clean (haiku reviewer).
- Task 5: complete, review clean. VTT anti-bot guard confirmed; imports clean (strconv excluded). Minor (no fix): yt-dlp --cookies placed after URL — harmless.
- Task 6: complete, review clean. Textarea confirmed inside `<form action="/admin">` (reaches postAdmin).
- **CHANGELOG.md NOTE for Task 8:** Task 3 added a "Stop button" entry AND Task 6 added a "Facebook cookies" entry under 2026-06-17. Task 8 must CONSOLIDATE into one clean dated entry covering both features (Stop + Facebook capture), removing any duplication — do not just append.
- Task 7: complete, review clean. Minors (no fix): import ordering (cosmetic); extra GetAppSettings read on FB path (spec-mandated).
- Task 8: complete, review clean. Docs synced (incl. zh-Hant), CHANGELOG consolidated, Stop smoke passed, real DB untouched, artifacts cleaned.
- FINAL whole-feature review (opus): READY TO MERGE. Found 1 Important race (stopped-note resurrection: a provider call completing between Cancel() and an unconditional DB write could overwrite Stopped→ready, leaving status='ready',stopped=1).
- FIX applied: 6 `ctx.Err()` guards added in worker.run() before each unconditional DB write (ApplyAIResult ×3, SetNoteTags, SetNoteCategory, ReplaceChunks). Build/vet/full-suite green. CHANGELOG got a "Fixed" note.
- ALL TASKS COMPLETE. Final: build OK, vet OK, all tests pass, real DB (Jun 10) untouched.
